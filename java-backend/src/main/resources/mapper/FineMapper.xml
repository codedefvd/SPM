<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.lms.mapper.FineMapper">

    <resultMap id="FineResultMap" type="com.team.lms.entity.Fine">
        <id property="id" column="id"/>
        <result property="amount" column="amount"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deleted" column="deleted"/>
        <association property="reader" javaType="com.team.lms.entity.User">
            <id property="id" column="reader_id"/>
            <result property="username" column="reader_username"/>
        </association>
        <association property="borrowRecord" javaType="com.team.lms.entity.BorrowRecord">
            <id property="id" column="borrow_record_id"/>
            <result property="dueDate" column="due_date"/>
            <result property="returnDate" column="return_date"/>
            <association property="book" javaType="com.team.lms.entity.Book">
                <id property="id" column="book_id"/>
                <result property="title" column="book_title"/>
            </association>
            <association property="bookCopy" javaType="com.team.lms.entity.BookCopy">
                <id property="id" column="book_copy_id"/>
                <result property="barcode" column="copy_barcode"/>
            </association>
        </association>
    </resultMap>

    <sql id="FineColumns">
        f.id, f.reader_id, f.borrow_record_id, f.amount, f.status, f.created_at, f.updated_at, f.deleted,
        u.username as reader_username,
        br.book_id, br.book_copy_id, br.due_date, br.return_date,
        b.title as book_title,
        bc.barcode as copy_barcode
    </sql>

    <select id="selectById" resultMap="FineResultMap">
        select <include refid="FineColumns"/>
        from fines f
        join users u on f.reader_id = u.id
        join borrow_records br on f.borrow_record_id = br.id
        join books b on br.book_id = b.id
        left join book_copies bc on br.book_copy_id = bc.id
        where f.id = #{id} and f.deleted = false
    </select>

    <select id="selectAll" resultMap="FineResultMap">
        select <include refid="FineColumns"/>
        from fines f
        join users u on f.reader_id = u.id
        join borrow_records br on f.borrow_record_id = br.id
        join books b on br.book_id = b.id
        left join book_copies bc on br.book_copy_id = bc.id
        where f.deleted = false
        order by f.id desc
    </select>

    <select id="selectByFilters" resultMap="FineResultMap">
        select <include refid="FineColumns"/>
        from fines f
        join users u on f.reader_id = u.id
        join borrow_records br on f.borrow_record_id = br.id
        join books b on br.book_id = b.id
        left join book_copies bc on br.book_copy_id = bc.id
        where f.deleted = false
        <if test="status != null and status != ''">
            and f.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
            and (
                lower(u.username) like #{keyword}
                or lower(b.title) like #{keyword}
                or lower(coalesce(bc.barcode, '')) like #{keyword}
                or cast(f.borrow_record_id as char) like #{keyword}
            )
        </if>
        order by
            case
                when f.status = 'UNPAID' then 0
                when f.status = 'WAIVED' then 1
                else 2
            end,
            f.updated_at desc,
            f.id desc
    </select>

    <select id="selectUnpaid" resultMap="FineResultMap">
        select <include refid="FineColumns"/>
        from fines f
        join users u on f.reader_id = u.id
        join borrow_records br on f.borrow_record_id = br.id
        join books b on br.book_id = b.id
        left join book_copies bc on br.book_copy_id = bc.id
        where f.deleted = false and f.status = 'UNPAID'
        order by f.id desc
    </select>

    <select id="selectByBorrowRecordId" resultMap="FineResultMap">
        select <include refid="FineColumns"/>
        from fines f
        join users u on f.reader_id = u.id
        join borrow_records br on f.borrow_record_id = br.id
        join books b on br.book_id = b.id
        left join book_copies bc on br.book_copy_id = bc.id
        where f.borrow_record_id = #{borrowRecordId} and f.deleted = false
        limit 1
    </select>

    <insert id="insert" parameterType="com.team.lms.entity.Fine" useGeneratedKeys="true" keyProperty="id">
        insert into fines(reader_id, borrow_record_id, amount, status, created_at, updated_at, deleted)
        values(#{reader.id}, #{borrowRecord.id}, #{amount}, #{status}, now(), now(), false)
    </insert>

    <update id="update" parameterType="com.team.lms.entity.Fine">
        update fines
        set amount = #{amount},
            status = #{status},
            updated_at = now()
        where id = #{id}
    </update>
</mapper>
