<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.lms.mapper.BookReviewMapper">

    <resultMap id="BookReviewResultMap" type="com.team.lms.entity.BookReview">
        <id property="id" column="id"/>
        <result property="ratingScore" column="rating_score"/>
        <result property="reviewContent" column="review_content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deleted" column="deleted"/>
        <association property="reader" javaType="com.team.lms.entity.User">
            <id property="id" column="reader_id"/>
            <result property="username" column="reader_username"/>
        </association>
        <association property="book" javaType="com.team.lms.entity.Book">
            <id property="id" column="book_id"/>
            <result property="title" column="book_title"/>
        </association>
    </resultMap>

    <sql id="ReviewColumns">
        r.id, r.reader_id, r.book_id, r.rating_score, r.review_content, r.created_at, r.updated_at, r.deleted,
        u.username as reader_username,
        b.title as book_title
    </sql>

    <select id="selectAll" resultMap="BookReviewResultMap">
        select <include refid="ReviewColumns"/>
        from book_reviews r
        join users u on r.reader_id = u.id
        join books b on r.book_id = b.id
        where r.deleted = false
        order by r.created_at desc, r.id desc
    </select>

    <select id="selectByBookId" resultMap="BookReviewResultMap">
        select <include refid="ReviewColumns"/>
        from book_reviews r
        join users u on r.reader_id = u.id
        join books b on r.book_id = b.id
        where r.book_id = #{bookId} and r.deleted = false
        order by r.created_at desc, r.id desc
    </select>

    <select id="selectById" resultMap="BookReviewResultMap">
        select <include refid="ReviewColumns"/>
        from book_reviews r
        join users u on r.reader_id = u.id
        join books b on r.book_id = b.id
        where r.id = #{id} and r.deleted = false
        limit 1
    </select>

    <select id="selectByReaderAndBookId" resultMap="BookReviewResultMap">
        select <include refid="ReviewColumns"/>
        from book_reviews r
        join users u on r.reader_id = u.id
        join books b on r.book_id = b.id
        where r.reader_id = #{readerId} and r.book_id = #{bookId} and r.deleted = false
        limit 1
    </select>

    <insert id="insert" parameterType="com.team.lms.entity.BookReview" useGeneratedKeys="true" keyProperty="id">
        insert into book_reviews(reader_id, book_id, rating_score, review_content, created_at, updated_at, deleted)
        values(#{reader.id}, #{book.id}, #{ratingScore}, #{reviewContent}, #{createdAt}, #{updatedAt}, false)
    </insert>

    <update id="update" parameterType="com.team.lms.entity.BookReview">
        update book_reviews
        set rating_score = #{ratingScore},
            review_content = #{reviewContent},
            updated_at = now()
        where id = #{id}
    </update>
</mapper>
